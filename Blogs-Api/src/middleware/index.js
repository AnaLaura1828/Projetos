const validateLogin = require('./validateLogin');
const validateUser = require('./validateUser');
const tokenValidade = require('./tokenValidade');
const validateCategories = require('./validateCategories');

module.exports = {
    validateLogin,
    validateUser,
    tokenValidade,
    validateCategories,
};